package com.movsisyan.program;

import com.movsisyan.module.Fraction;

public class Program {
    public static void main(String[] args) {
        Fraction f = new Fraction(1 / 2);
        Fraction[] fractions = {new Fraction(1 / 2), new Fraction(1 / 2),};

    }
}
